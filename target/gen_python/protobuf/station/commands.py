from __future__ import annotations

# This file is generated by `gremlin_py`.
# DO NOT EDIT.

import dataclasses as dataclasses
import typing as typing
import shared.gremlin_py.gremlin as gremlin
import target.gen_python.protobuf.station.drivers as drivers


# messages
@dataclasses.dataclass
class DriverCommand:
    # fields
    command_id: typing.Optional[bytes] = None
    type: drivers.StationCommandType = drivers.StationCommandType(0)
    body: typing.Optional[bytes] = None

    def calc_protobuf_size(self) -> int:
        res = 0
        if self.command_id is not None and len(self.command_id) > 0:
            bytes_len = len(self.command_id)
            bytes_len_size = ((bytes_len | 1).bit_length() + 6) // 7
            res += 1 + bytes_len_size + bytes_len
        if self.type != drivers.StationCommandType(0):
            res += 1 + (((self.type.value | 1).bit_length() + 6) // 7)
        if self.body is not None and len(self.body) > 0:
            bytes_len = len(self.body)
            bytes_len_size = ((bytes_len | 1).bit_length() + 6) // 7
            res += 1 + bytes_len_size + bytes_len
        return res

    def encode(self) -> bytes:
        size = self.calc_protobuf_size()
        if size == 0:
            return b''
        buf = bytearray(size)
        writer = gremlin.Writer(buf)
        self.encode_to(writer)
        return bytes(buf)

    def encode_to(self, target: typing.Union[gremlin.Writer, gremlin.StreamingWriter]):
        if self.command_id is not None and len(self.command_id) > 0:
            target.append_bytes(b'\n', self.command_id)
        if self.type != drivers.StationCommandType(0):
            target.append_int32(b'\x10', self.type.value)
        if self.body is not None and len(self.body) > 0:
            target.append_bytes(b'\x1a', self.body)


class DriverCommandReader:
    def __init__(self, src: memoryview):
        self._command_id: typing.Optional[memoryview] = None
        self._type: drivers.StationCommandType = drivers.StationCommandType(0)
        self._body: typing.Optional[memoryview] = None

        if not src:
            return
        self._buf = gremlin.Reader(src)
        offset = 0
        while offset < len(src):
            tag = self._buf.read_tag_at(offset)
            offset += tag.size
            match tag.number:
                case 1:
                    result = self._buf.read_bytes_view(offset)
                    offset += result.size
                    self._command_id = result.value
                case 2:
                    result = self._buf.read_int32(offset)
                    offset += result.size
                    self._type = drivers.StationCommandType(result.value)
                case 3:
                    result = self._buf.read_bytes_view(offset)
                    offset += result.size
                    self._body = result.value
                case _:
                    offset = self._buf.skip_data(offset, tag.wire)

    def get_command_id(self) -> memoryview:
        return self._command_id if self._command_id is not None else memoryview(b'')

    def get_type(self) -> drivers.StationCommandType:
        return self._type

    def get_body(self) -> memoryview:
        return self._body if self._body is not None else memoryview(b'')


@dataclasses.dataclass
class StationCommandsPack:
    # fields
    inference_state_id: typing.Optional[bytes] = None
    pack_id: typing.Optional[bytes] = None
    commands: list[DriverCommand | None] | None = None
    tags: typing.Optional[list[typing.Optional[bytes]]] = None

    def calc_protobuf_size(self) -> int:
        res = 0
        if self.inference_state_id is not None and len(self.inference_state_id) > 0:
            bytes_len = len(self.inference_state_id)
            bytes_len_size = ((bytes_len | 1).bit_length() + 6) // 7
            res += 1 + bytes_len_size + bytes_len
        if self.pack_id is not None and len(self.pack_id) > 0:
            bytes_len = len(self.pack_id)
            bytes_len_size = ((bytes_len | 1).bit_length() + 6) // 7
            res += 1 + bytes_len_size + bytes_len
        if self.commands is not None:
            for v in self.commands:
                res += 1
                if v is not None:
                    size = v.calc_protobuf_size()
                    res += (((size | 1).bit_length() + 6) // 7) + size
                else:
                    res += 1
        
        if self.tags:
            for v in self.tags:
                res += 2
                if v is not None:
                    res += (((len(v) | 1).bit_length() + 6) // 7) + len(v)
                else:
                    res += 1
        return res

    def encode(self) -> bytes:
        size = self.calc_protobuf_size()
        if size == 0:
            return b''
        buf = bytearray(size)
        writer = gremlin.Writer(buf)
        self.encode_to(writer)
        return bytes(buf)

    def encode_to(self, target: typing.Union[gremlin.Writer, gremlin.StreamingWriter]):
        if self.inference_state_id is not None and len(self.inference_state_id) > 0:
            target.append_bytes(b'\n', self.inference_state_id)
        if self.pack_id is not None and len(self.pack_id) > 0:
            target.append_bytes(b'\x12', self.pack_id)
        if self.commands is not None:
            for v in self.commands:
                if v is not None:
                    size = v.calc_protobuf_size()
                    target.append_bytes_size_with_tag(b'R', size)
                    v.encode_to(target)
                else:
                    target.append_bytes_size_with_tag(b'R', 0)
        
        if self.tags:
            for v in self.tags:
                if v is not None:
                    target.append_bytes(b'\xa2\x01', v.encode('utf-8'))
                else:
                    target.append_bytes_size_with_tag(b'\xa2\x01', 0)


class StationCommandsPackReader:
    def __init__(self, src: memoryview):
        self._inference_state_id: typing.Optional[memoryview] = None
        self._pack_id: typing.Optional[memoryview] = None
        self._commands_bufs: list[memoryview] | None = None
        self._tags: typing.Optional[list[str]] = None

        if not src:
            return
        self._buf = gremlin.Reader(src)
        offset = 0
        while offset < len(src):
            tag = self._buf.read_tag_at(offset)
            offset += tag.size
            match tag.number:
                case 1:
                    result = self._buf.read_bytes_view(offset)
                    offset += result.size
                    self._inference_state_id = result.value
                case 2:
                    result = self._buf.read_bytes_view(offset)
                    offset += result.size
                    self._pack_id = result.value
                case 10:
                    result = self._buf.read_bytes_view(offset)
                    offset += result.size
                    if self._commands_bufs is None:
                        self._commands_bufs = []
                    self._commands_bufs.append(result.value)
            
                case 20:
                    result =  self._buf.read_bytes_view(offset)
                    offset += result.size
                    if self._tags is None:
                        self._tags = []
                    self._tags.append(result.value.tobytes().decode('utf-8'))
                case _:
                    offset = self._buf.skip_data(offset, tag.wire)

    def get_inference_state_id(self) -> memoryview:
        return self._inference_state_id if self._inference_state_id is not None else memoryview(b'')

    def get_pack_id(self) -> memoryview:
        return self._pack_id if self._pack_id is not None else memoryview(b'')

    def get_commands(self) -> list[DriverCommandReader]:
        if self._commands_bufs is not None:
            result: list[DriverCommandReader] = []
            for buf in self._commands_bufs:
                result.append(DriverCommandReader(buf))
            return result
        return []
    

    def get_tags(self) -> list[str]:
        return self._tags if self._tags is not None else []



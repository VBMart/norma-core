from __future__ import annotations

# This file is generated by `gremlin_py`.
# DO NOT EDIT.

import dataclasses as dataclasses
import typing as typing
import shared.gremlin_py.gremlin as gremlin
import target.gen_python.protobuf.station.drivers as drivers


# messages
@dataclasses.dataclass
class InferenceRx:
    # fields
    local_stamp_ns: int = 0
    monotonic_stamp_ns: int = 0
    app_start_id: int = 0
    entries: list[InferenceRx_Entry | None] | None = None

    def calc_protobuf_size(self) -> int:
        res = 0
        if self.local_stamp_ns != 0:
            res += 1 + gremlin.sizes.size_varint(self.local_stamp_ns)
        if self.monotonic_stamp_ns != 0:
            res += 1 + gremlin.sizes.size_varint(self.monotonic_stamp_ns)
        if self.app_start_id != 0:
            res += 1 + gremlin.sizes.size_varint(self.app_start_id)
        if self.entries is not None:
            for v in self.entries:
                res += 1
                if v is not None:
                    size = v.calc_protobuf_size()
                    res += (((size | 1).bit_length() + 6) // 7) + size
                else:
                    res += 1
        
        return res

    def encode(self) -> bytes:
        size = self.calc_protobuf_size()
        if size == 0:
            return b''
        buf = bytearray(size)
        writer = gremlin.Writer(buf)
        self.encode_to(writer)
        return bytes(buf)

    def encode_to(self, target: typing.Union[gremlin.Writer, gremlin.StreamingWriter]):
        if self.local_stamp_ns != 0:
            target.append_uint64(b'\x08', self.local_stamp_ns)
        if self.monotonic_stamp_ns != 0:
            target.append_uint64(b'\x10', self.monotonic_stamp_ns)
        if self.app_start_id != 0:
            target.append_uint64(b'\x18', self.app_start_id)
        if self.entries is not None:
            for v in self.entries:
                if v is not None:
                    size = v.calc_protobuf_size()
                    target.append_bytes_size_with_tag(b'R', size)
                    v.encode_to(target)
                else:
                    target.append_bytes_size_with_tag(b'R', 0)
        


class InferenceRxReader:
    def __init__(self, src: memoryview):
        self._local_stamp_ns: int = 0
        self._monotonic_stamp_ns: int = 0
        self._app_start_id: int = 0
        self._entries_bufs: list[memoryview] | None = None

        if not src:
            return
        self._buf = gremlin.Reader(src)
        offset = 0
        while offset < len(src):
            tag = self._buf.read_tag_at(offset)
            offset += tag.size
            match tag.number:
                case 1:
                    result = self._buf.read_uint64(offset)
                    offset += result.size
                    self._local_stamp_ns = result.value
                case 2:
                    result = self._buf.read_uint64(offset)
                    offset += result.size
                    self._monotonic_stamp_ns = result.value
                case 3:
                    result = self._buf.read_uint64(offset)
                    offset += result.size
                    self._app_start_id = result.value
                case 10:
                    result = self._buf.read_bytes_view(offset)
                    offset += result.size
                    if self._entries_bufs is None:
                        self._entries_bufs = []
                    self._entries_bufs.append(result.value)
            
                case _:
                    offset = self._buf.skip_data(offset, tag.wire)

    def get_local_stamp_ns(self) -> int:
        return self._local_stamp_ns

    def get_monotonic_stamp_ns(self) -> int:
        return self._monotonic_stamp_ns

    def get_app_start_id(self) -> int:
        return self._app_start_id

    def get_entries(self) -> list[InferenceRx_EntryReader]:
        if self._entries_bufs is not None:
            result: list[InferenceRx_EntryReader] = []
            for buf in self._entries_bufs:
                result.append(InferenceRx_EntryReader(buf))
            return result
        return []
    


@dataclasses.dataclass
class InferenceRx_Entry:
    # fields
    queue: typing.Optional[bytes] = None
    ptr: typing.Optional[bytes] = None
    type: drivers.QueueDataType = drivers.QueueDataType(0)

    def calc_protobuf_size(self) -> int:
        res = 0
        if self.queue is not None and len(self.queue) > 0:
            bytes_len = len(self.queue)
            bytes_len_size = ((bytes_len | 1).bit_length() + 6) // 7
            res += 1 + bytes_len_size + bytes_len
        if self.ptr is not None and len(self.ptr) > 0:
            bytes_len = len(self.ptr)
            bytes_len_size = ((bytes_len | 1).bit_length() + 6) // 7
            res += 1 + bytes_len_size + bytes_len
        if self.type != drivers.QueueDataType(0):
            res += 1 + (((self.type.value | 1).bit_length() + 6) // 7)
        return res

    def encode(self) -> bytes:
        size = self.calc_protobuf_size()
        if size == 0:
            return b''
        buf = bytearray(size)
        writer = gremlin.Writer(buf)
        self.encode_to(writer)
        return bytes(buf)

    def encode_to(self, target: typing.Union[gremlin.Writer, gremlin.StreamingWriter]):
        if self.queue is not None and len(self.queue) > 0:
            target.append_bytes(b'\n', self.queue.encode('utf-8'))
        if self.ptr is not None and len(self.ptr) > 0:
            target.append_bytes(b'\x12', self.ptr)
        if self.type != drivers.QueueDataType(0):
            target.append_int32(b'\x18', self.type.value)


class InferenceRx_EntryReader:
    def __init__(self, src: memoryview):
        self._queue: typing.Optional[str] = None
        self._ptr: typing.Optional[memoryview] = None
        self._type: drivers.QueueDataType = drivers.QueueDataType(0)

        if not src:
            return
        self._buf = gremlin.Reader(src)
        offset = 0
        while offset < len(src):
            tag = self._buf.read_tag_at(offset)
            offset += tag.size
            match tag.number:
                case 1:
                    result = self._buf.read_bytes_view(offset)
                    offset += result.size
                    self._queue = result.value.tobytes().decode('utf-8')
                case 2:
                    result = self._buf.read_bytes_view(offset)
                    offset += result.size
                    self._ptr = result.value
                case 3:
                    result = self._buf.read_int32(offset)
                    offset += result.size
                    self._type = drivers.QueueDataType(result.value)
                case _:
                    offset = self._buf.skip_data(offset, tag.wire)

    def get_queue(self) -> str:
        return self._queue if self._queue is not None else ''

    def get_ptr(self) -> memoryview:
        return self._ptr if self._ptr is not None else memoryview(b'')

    def get_type(self) -> drivers.QueueDataType:
        return self._type



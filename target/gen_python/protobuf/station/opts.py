from __future__ import annotations

# This file is generated by `gremlin_py`.
# DO NOT EDIT.

import dataclasses as dataclasses
import typing as typing
import shared.gremlin_py.gremlin as gremlin


# messages
@dataclasses.dataclass
class OptValue:
    # fields
    uint_value: int = 0
    bool_value: bool = False
    bytes_value: typing.Optional[bytes] = None
    string_value: typing.Optional[bytes] = None

    def calc_protobuf_size(self) -> int:
        res = 0
        if self.uint_value != 0:
            res += 1 + gremlin.sizes.size_varint(self.uint_value)
        if self.bool_value:
            res += 2
        if self.bytes_value is not None and len(self.bytes_value) > 0:
            bytes_len = len(self.bytes_value)
            bytes_len_size = ((bytes_len | 1).bit_length() + 6) // 7
            res += 1 + bytes_len_size + bytes_len
        if self.string_value is not None and len(self.string_value) > 0:
            bytes_len = len(self.string_value)
            bytes_len_size = ((bytes_len | 1).bit_length() + 6) // 7
            res += 1 + bytes_len_size + bytes_len
        return res

    def encode(self) -> bytes:
        size = self.calc_protobuf_size()
        if size == 0:
            return b''
        buf = bytearray(size)
        writer = gremlin.Writer(buf)
        self.encode_to(writer)
        return bytes(buf)

    def encode_to(self, target: typing.Union[gremlin.Writer, gremlin.StreamingWriter]):
        if self.uint_value != 0:
            target.append_uint64(b'\x08', self.uint_value)
        if self.bool_value:
            target.append_bool(b'\x10', self.bool_value)
        if self.bytes_value is not None and len(self.bytes_value) > 0:
            target.append_bytes(b'\x1a', self.bytes_value)
        if self.string_value is not None and len(self.string_value) > 0:
            target.append_bytes(b'"', self.string_value.encode('utf-8'))


class OptValueReader:
    def __init__(self, src: memoryview):
        self._uint_value: int = 0
        self._bool_value: bool = False
        self._bytes_value: typing.Optional[memoryview] = None
        self._string_value: typing.Optional[str] = None

        if not src:
            return
        self._buf = gremlin.Reader(src)
        offset = 0
        while offset < len(src):
            tag = self._buf.read_tag_at(offset)
            offset += tag.size
            match tag.number:
                case 1:
                    result = self._buf.read_uint64(offset)
                    offset += result.size
                    self._uint_value = result.value
                case 2:
                    result = self._buf.read_bool(offset)
                    offset += result.size
                    self._bool_value = result.value
                case 3:
                    result = self._buf.read_bytes_view(offset)
                    offset += result.size
                    self._bytes_value = result.value
                case 4:
                    result = self._buf.read_bytes_view(offset)
                    offset += result.size
                    self._string_value = result.value.tobytes().decode('utf-8')
                case _:
                    offset = self._buf.skip_data(offset, tag.wire)

    def get_uint_value(self) -> int:
        return self._uint_value

    def get_bool_value(self) -> bool:
        return self._bool_value

    def get_bytes_value(self) -> memoryview:
        return self._bytes_value if self._bytes_value is not None else memoryview(b'')

    def get_string_value(self) -> str:
        return self._string_value if self._string_value is not None else ''



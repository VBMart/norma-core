from __future__ import annotations

# This file is generated by `gremlin_py`.
# DO NOT EDIT.

import dataclasses as dataclasses
import typing as typing
import shared.gremlin_py.gremlin as gremlin


# messages
@dataclasses.dataclass
class Frame:
    # fields
    global_frame_id: typing.Optional[bytes] = None
    monotonic_stamp_ns: int = 0
    joints: list[Joint | None] | None = None
    images: list[Image | None] | None = None

    def calc_protobuf_size(self) -> int:
        res = 0
        if self.global_frame_id is not None and len(self.global_frame_id) > 0:
            bytes_len = len(self.global_frame_id)
            bytes_len_size = ((bytes_len | 1).bit_length() + 6) // 7
            res += 1 + bytes_len_size + bytes_len
        if self.monotonic_stamp_ns != 0:
            res += 1 + gremlin.sizes.size_varint(self.monotonic_stamp_ns)
        if self.joints is not None:
            for v in self.joints:
                res += 1
                if v is not None:
                    size = v.calc_protobuf_size()
                    res += (((size | 1).bit_length() + 6) // 7) + size
                else:
                    res += 1
        
        if self.images is not None:
            for v in self.images:
                res += 1
                if v is not None:
                    size = v.calc_protobuf_size()
                    res += (((size | 1).bit_length() + 6) // 7) + size
                else:
                    res += 1
        
        return res

    def encode(self) -> bytes:
        size = self.calc_protobuf_size()
        if size == 0:
            return b''
        buf = bytearray(size)
        writer = gremlin.Writer(buf)
        self.encode_to(writer)
        return bytes(buf)

    def encode_to(self, target: typing.Union[gremlin.Writer, gremlin.StreamingWriter]):
        if self.global_frame_id is not None and len(self.global_frame_id) > 0:
            target.append_bytes(b'\n', self.global_frame_id)
        if self.monotonic_stamp_ns != 0:
            target.append_uint64(b'\x10', self.monotonic_stamp_ns)
        if self.joints is not None:
            for v in self.joints:
                if v is not None:
                    size = v.calc_protobuf_size()
                    target.append_bytes_size_with_tag(b'R', size)
                    v.encode_to(target)
                else:
                    target.append_bytes_size_with_tag(b'R', 0)
        
        if self.images is not None:
            for v in self.images:
                if v is not None:
                    size = v.calc_protobuf_size()
                    target.append_bytes_size_with_tag(b'Z', size)
                    v.encode_to(target)
                else:
                    target.append_bytes_size_with_tag(b'Z', 0)
        


class FrameReader:
    def __init__(self, src: memoryview):
        self._global_frame_id: typing.Optional[memoryview] = None
        self._monotonic_stamp_ns: int = 0
        self._joints_bufs: list[memoryview] | None = None
        self._images_bufs: list[memoryview] | None = None

        if not src:
            return
        self._buf = gremlin.Reader(src)
        offset = 0
        while offset < len(src):
            tag = self._buf.read_tag_at(offset)
            offset += tag.size
            match tag.number:
                case 1:
                    result = self._buf.read_bytes_view(offset)
                    offset += result.size
                    self._global_frame_id = result.value
                case 2:
                    result = self._buf.read_uint64(offset)
                    offset += result.size
                    self._monotonic_stamp_ns = result.value
                case 10:
                    result = self._buf.read_bytes_view(offset)
                    offset += result.size
                    if self._joints_bufs is None:
                        self._joints_bufs = []
                    self._joints_bufs.append(result.value)
            
                case 11:
                    result = self._buf.read_bytes_view(offset)
                    offset += result.size
                    if self._images_bufs is None:
                        self._images_bufs = []
                    self._images_bufs.append(result.value)
            
                case _:
                    offset = self._buf.skip_data(offset, tag.wire)

    def get_global_frame_id(self) -> memoryview:
        return self._global_frame_id if self._global_frame_id is not None else memoryview(b'')

    def get_monotonic_stamp_ns(self) -> int:
        return self._monotonic_stamp_ns

    def get_joints(self) -> list[JointReader]:
        if self._joints_bufs is not None:
            result: list[JointReader] = []
            for buf in self._joints_bufs:
                result.append(JointReader(buf))
            return result
        return []
    

    def get_images(self) -> list[ImageReader]:
        if self._images_bufs is not None:
            result: list[ImageReader] = []
            for buf in self._images_bufs:
                result.append(ImageReader(buf))
            return result
        return []
    


@dataclasses.dataclass
class Joint:
    # fields
    range_min: int = 0
    range_max: int = 0
    position: int = 0
    position_norm: float = 0.0
    goal: int = 0
    goal_norm: float = 0.0
    current_ma: int = 0
    velocity: int = 0
    monotonic_stamp_ns: int = 0

    def calc_protobuf_size(self) -> int:
        res = 0
        if self.range_min != 0:
            res += 1 + gremlin.sizes.size_varint(self.range_min)
        if self.range_max != 0:
            res += 1 + gremlin.sizes.size_varint(self.range_max)
        if self.position != 0:
            res += 1 + gremlin.sizes.size_varint(self.position)
        if self.position_norm != 0.0:
            res += 5
        if self.goal != 0:
            res += 1 + gremlin.sizes.size_varint(self.goal)
        if self.goal_norm != 0.0:
            res += 5
        if self.current_ma != 0:
            res += 1 + gremlin.sizes.size_varint(self.current_ma)
        if self.velocity != 0:
            res += 1 + gremlin.sizes.size_varint(self.velocity)
        if self.monotonic_stamp_ns != 0:
            res += 1 + gremlin.sizes.size_varint(self.monotonic_stamp_ns)
        return res

    def encode(self) -> bytes:
        size = self.calc_protobuf_size()
        if size == 0:
            return b''
        buf = bytearray(size)
        writer = gremlin.Writer(buf)
        self.encode_to(writer)
        return bytes(buf)

    def encode_to(self, target: typing.Union[gremlin.Writer, gremlin.StreamingWriter]):
        if self.range_min != 0:
            target.append_uint32(b'\x08', self.range_min)
        if self.range_max != 0:
            target.append_uint32(b'\x10', self.range_max)
        if self.position != 0:
            target.append_uint32(b'\x18', self.position)
        if self.position_norm != 0.0:
            target.append_float32(b'%', self.position_norm)
        if self.goal != 0:
            target.append_uint32(b'(', self.goal)
        if self.goal_norm != 0.0:
            target.append_float32(b'5', self.goal_norm)
        if self.current_ma != 0:
            target.append_uint32(b'8', self.current_ma)
        if self.velocity != 0:
            target.append_uint32(b'@', self.velocity)
        if self.monotonic_stamp_ns != 0:
            target.append_uint64(b'H', self.monotonic_stamp_ns)


class JointReader:
    def __init__(self, src: memoryview):
        self._range_min: int = 0
        self._range_max: int = 0
        self._position: int = 0
        self._position_norm: float = 0.0
        self._goal: int = 0
        self._goal_norm: float = 0.0
        self._current_ma: int = 0
        self._velocity: int = 0
        self._monotonic_stamp_ns: int = 0

        if not src:
            return
        self._buf = gremlin.Reader(src)
        offset = 0
        while offset < len(src):
            tag = self._buf.read_tag_at(offset)
            offset += tag.size
            match tag.number:
                case 1:
                    result = self._buf.read_uint32(offset)
                    offset += result.size
                    self._range_min = result.value
                case 2:
                    result = self._buf.read_uint32(offset)
                    offset += result.size
                    self._range_max = result.value
                case 3:
                    result = self._buf.read_uint32(offset)
                    offset += result.size
                    self._position = result.value
                case 4:
                    result = self._buf.read_float32(offset)
                    offset += result.size
                    self._position_norm = result.value
                case 5:
                    result = self._buf.read_uint32(offset)
                    offset += result.size
                    self._goal = result.value
                case 6:
                    result = self._buf.read_float32(offset)
                    offset += result.size
                    self._goal_norm = result.value
                case 7:
                    result = self._buf.read_uint32(offset)
                    offset += result.size
                    self._current_ma = result.value
                case 8:
                    result = self._buf.read_uint32(offset)
                    offset += result.size
                    self._velocity = result.value
                case 9:
                    result = self._buf.read_uint64(offset)
                    offset += result.size
                    self._monotonic_stamp_ns = result.value
                case _:
                    offset = self._buf.skip_data(offset, tag.wire)

    def get_range_min(self) -> int:
        return self._range_min

    def get_range_max(self) -> int:
        return self._range_max

    def get_position(self) -> int:
        return self._position

    def get_position_norm(self) -> float:
        return self._position_norm

    def get_goal(self) -> int:
        return self._goal

    def get_goal_norm(self) -> float:
        return self._goal_norm

    def get_current_ma(self) -> int:
        return self._current_ma

    def get_velocity(self) -> int:
        return self._velocity

    def get_monotonic_stamp_ns(self) -> int:
        return self._monotonic_stamp_ns


@dataclasses.dataclass
class Image:
    # fields
    jpeg: typing.Optional[bytes] = None
    monotonic_stamp_ns: int = 0

    def calc_protobuf_size(self) -> int:
        res = 0
        if self.jpeg is not None and len(self.jpeg) > 0:
            bytes_len = len(self.jpeg)
            bytes_len_size = ((bytes_len | 1).bit_length() + 6) // 7
            res += 1 + bytes_len_size + bytes_len
        if self.monotonic_stamp_ns != 0:
            res += 1 + gremlin.sizes.size_varint(self.monotonic_stamp_ns)
        return res

    def encode(self) -> bytes:
        size = self.calc_protobuf_size()
        if size == 0:
            return b''
        buf = bytearray(size)
        writer = gremlin.Writer(buf)
        self.encode_to(writer)
        return bytes(buf)

    def encode_to(self, target: typing.Union[gremlin.Writer, gremlin.StreamingWriter]):
        if self.jpeg is not None and len(self.jpeg) > 0:
            target.append_bytes(b'\n', self.jpeg)
        if self.monotonic_stamp_ns != 0:
            target.append_uint64(b'\x10', self.monotonic_stamp_ns)


class ImageReader:
    def __init__(self, src: memoryview):
        self._jpeg: typing.Optional[memoryview] = None
        self._monotonic_stamp_ns: int = 0

        if not src:
            return
        self._buf = gremlin.Reader(src)
        offset = 0
        while offset < len(src):
            tag = self._buf.read_tag_at(offset)
            offset += tag.size
            match tag.number:
                case 1:
                    result = self._buf.read_bytes_view(offset)
                    offset += result.size
                    self._jpeg = result.value
                case 2:
                    result = self._buf.read_uint64(offset)
                    offset += result.size
                    self._monotonic_stamp_ns = result.value
                case _:
                    offset = self._buf.skip_data(offset, tag.wire)

    def get_jpeg(self) -> memoryview:
        return self._jpeg if self._jpeg is not None else memoryview(b'')

    def get_monotonic_stamp_ns(self) -> int:
        return self._monotonic_stamp_ns


